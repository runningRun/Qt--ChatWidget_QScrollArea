2015.12.14
those codes have realized the following functions:
1. the frame has been built, text,picture,voice and video could be supported, only need to code in those classes: TextMsgContent, ImageMsgContent, voiceMsgContent and videoMsgContent
2. the size of text message has matched the widget's size
